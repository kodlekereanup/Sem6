#include "Board.h"

class HillCLimbingRandomRestart {
public:
	//TODO:
	/*
	Generate a random state
	i.e Place the queens randomly
	within the boundaries of the board
	*/
	int steps = 0;
	int randStarts = 0;

	bool ifConflict(curBoardState, queens);

};